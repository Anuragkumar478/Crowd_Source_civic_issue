API latency — before → after
P95 latency
API error rate
Response payload size
MongoDB query performance
Lighthouse Performance
LCP / CLS / INP
Image size reduction
Concurrent users + error rate
GenAI classification accuracy + latency